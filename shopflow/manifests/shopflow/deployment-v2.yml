apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service-v2
  namespace: shopflow-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels: 
        app: product-service
        version: v2
    spec:
      containers:
        - name: shopflow-product
          image: hashicorp/http-echo
          args:
            - "-text"
            - "product-service-v2 response"
          ports:
            - containerPort: 5678
          envFrom:
            - configMapRef:
                name: shopflow-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopflow-secret
                  key: DB_PASSWORD
          resources:
            requests: 
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 130Mi
      